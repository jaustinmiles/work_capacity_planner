# .env.server.example — Copy to .env.server and fill in values

# Server configuration
TASK_PLANNER_MODE=server
TASK_PLANNER_PORT=3001

# REQUIRED for remote access: generate with `openssl rand -hex 32`
TASK_PLANNER_API_KEY=

# Allowed CORS origins (comma-separated prefixes or wildcard domain patterns)
# For Cloudflare quick tunnel: *.trycloudflare.com
# For named tunnel: https://tasks.yourdomain.com
TASK_PLANNER_CORS_ORIGINS=

# AI API keys
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
